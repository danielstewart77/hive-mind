# Pinned because a trainer that silently changes behaviour between runs
# makes every comparison between two fine-tunes meaningless. torch comes
# from the base image, which already carries the matching CUDA runtime.
transformers==4.57.1
peft==0.17.1
trl==0.24.0
datasets==4.0.0
accelerate==1.10.1
bitsandbytes==0.48.1
sentencepiece==0.2.0
protobuf==6.31.1
safetensors==0.6.2
